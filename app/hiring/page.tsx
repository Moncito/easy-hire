import Link from "next/link";
import { redirect } from "next/navigation";
import { ArrowLeft, ArrowRight, Bell, BriefcaseBusiness, Building2, ShieldCheck, Sparkles, UsersRound } from "lucide-react";
import { auth } from "@/Auth";
import { getHiringWorkspacesForUser } from "@/lib/collaborative-hiring";
import EmployerAvatar from "@/components/employer/ui/EmployerAvatar";

const roleDescription: Record<string, string> = {
  OWNER: "Full workspace control",
  RECRUITER: "Candidate and pipeline access",
  HIRING_MANAGER: "Assigned-role review access",
  VIEWER: "Read-only workspace access",
};

export default async function HiringWorkspacesPage() {
  const session = await auth();
  if (!session?.user) redirect("/login?callbackUrl=/hiring");
  const workspaces = await getHiringWorkspacesForUser(session.user.id);
  const accountHref = session.user.role === "SEEKER" ? "/seeker/dashboard" : "/employer/dashboard";

  return <main className="relative flex min-h-screen items-center overflow-hidden bg-mist px-5 py-8 sm:px-8"><div className="pointer-events-none absolute -left-24 top-1/4 h-72 w-72 rounded-full bg-teal/[0.07] blur-3xl" /><div className="pointer-events-none absolute -right-24 bottom-1/4 h-72 w-72 rounded-full bg-marigold/[0.10] blur-3xl" /><div className="relative mx-auto w-full max-w-6xl"><div className="mb-7 flex items-center justify-between"><Link href={accountHref} className="inline-flex items-center gap-2 text-sm font-semibold text-ink/60 transition hover:text-teal"><ArrowLeft className="h-4 w-4" />My EasyHire account</Link><div className="flex items-center gap-3"><Link href="/hiring/notifications" className="rounded-full p-2 text-ink/55 transition hover:bg-white hover:text-teal" aria-label="Hiring notifications"><Bell className="h-4 w-4" /></Link><div className="flex items-center gap-2 text-sm font-bold text-ink"><span className="relative flex h-8 w-8 overflow-hidden rounded-full"><span className="absolute inset-0 bg-marigold" style={{ clipPath: "polygon(0 0, 100% 0, 0 100%)" }} /><span className="absolute inset-0 bg-teal" style={{ clipPath: "polygon(100% 0, 100% 100%, 0 100%)" }} /></span><span className="font-display text-lg font-black tracking-tighter">EasyHire</span></div></div></div><div className="grid items-center gap-10 lg:grid-cols-[.85fr_1.15fr] lg:gap-16"><section><p className="inline-flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.16em] text-[#9A5B12]"><Sparkles className="h-3.5 w-3.5" />Collaborative hiring</p><h1 className="mt-3 font-display text-4xl font-black tracking-tighter text-ink sm:text-5xl sm:leading-[0.93]">Choose where<br />you’re hiring.</h1><p className="mt-5 max-w-md text-sm leading-6 text-ink/60">Company workspaces are private and role-based. Your personal seeker profile, applications, and job search remain entirely separate.</p><div className="mt-7 flex items-start gap-3 border-l-2 border-teal bg-teal/[0.05] px-4 py-3"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-teal" /><p className="text-sm leading-6 text-ink/65">You can move between your account and any hiring team at any time.</p></div><Link href="/jobs" className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-ink/65 transition hover:text-teal"><BriefcaseBusiness className="h-4 w-4" />Browse public jobs</Link></section><section aria-label="Available hiring workspaces">{workspaces.length === 0 ? <div className="rounded-[24px] border border-ink/8 bg-white p-8 shadow-[0_16px_40px_rgba(32,36,43,0.06)]"><UsersRound className="h-7 w-7 text-teal" /><h2 className="mt-5 font-display text-2xl font-bold tracking-tight text-ink">No active workspaces yet</h2><p className="mt-2 text-sm leading-6 text-ink/55">When a company invites you to collaborate, your private hiring workspace will appear here.</p></div> : <div className="overflow-hidden rounded-[24px] border border-ink/8 bg-white shadow-[0_16px_40px_rgba(32,36,43,0.06)]"><div className="flex items-center justify-between border-b border-ink/7 px-6 py-5"><div><p className="text-xs font-bold uppercase tracking-[0.13em] text-ink/45">Your workspaces</p><h2 className="mt-1 font-display text-2xl font-bold tracking-tight text-ink">Hiring teams</h2></div><span className="rounded-full bg-teal/10 px-3 py-1 text-xs font-bold text-teal">{workspaces.length}</span></div><ul className="divide-y divide-ink/7">{workspaces.map((workspace) => <li key={workspace.id}><Link href={`/hiring/${workspace.companyId}/team`} className="group flex items-center gap-4 px-6 py-5 transition hover:bg-mist/60"><EmployerAvatar name={workspace.company.companyName} imageUrl={workspace.company.logoUrl} size="lg" shape="rounded" className="!h-14 !w-14 !rounded-2xl" fallbackClassName="bg-marigold/20 text-[#81510d] text-lg font-bold" /><div className="min-w-0 flex-1"><p className="truncate font-display text-xl font-bold tracking-tight text-ink">{workspace.company.companyName}</p><p className="mt-1 text-sm text-ink/50">{roleDescription[workspace.role] ?? workspace.role.replace(/_/g, " ")}</p><span className="mt-3 inline-flex items-center gap-1.5 rounded-full bg-ink/[0.04] px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.12em] text-ink/55"><Building2 className="h-3 w-3" />{workspace.role.replace(/_/g, " ")}</span></div><ArrowRight className="h-5 w-5 text-ink/30 transition group-hover:translate-x-1 group-hover:text-teal" /></Link></li>)}</ul></div>}</section></div></div></main>;
}
