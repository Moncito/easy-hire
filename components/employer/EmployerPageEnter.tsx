"use client";

export default function EmployerPageEnter({ children }: { children: React.ReactNode }) {
  return <div className="employer-page-enter flex h-full min-h-0 flex-1 flex-col">{children}</div>;
}
